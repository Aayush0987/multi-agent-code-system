// Where the API lives. Leave empty when the API serves this page itself
// (local use). For a separate frontend host such as Vercel, set it to the
// backend's URL, e.g. "https://your-backend.onrender.com".
window.APP_CONFIG = { apiBase: "https://multi-agent-code-api.onrender.com" };
